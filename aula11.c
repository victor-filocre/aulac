#include <stdio.h>

int main()
{
    int contador = 1;
    
    while (contador <= 5)               // enquanto o contador for menor ou igual a 5 executar a funcao no bloco de comando

    {
        printf ("%i\n", contador);
        ++contador;             // contador = contador +1
    }

                    //  = definindo      == comparando
    return 0;
}